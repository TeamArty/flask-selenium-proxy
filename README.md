# Flask Selenium Proxy
Flask app to get the page source of an url using Selenium. Added a custom user agent to bypass CloudFlare's anti-bot system.
- A few lines of code
- Made specifically to be hosted on **render.com**